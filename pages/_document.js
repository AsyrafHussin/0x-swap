import { Html, Head, Main, NextScript } from "next/document";

const Document = () => (
  <Html>
    <Head />
    <body className="bg-primary-content h-screen">
      <Main />
      <NextScript />
    </body>
  </Html>
);

export default Document;
